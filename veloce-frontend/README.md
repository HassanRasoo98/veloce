# Veloce frontend

Next.js app (App Router) in this directory.

**Architecture, data model, caching, AI pipeline, AI-tool disclosure, and roadmap** for reviewers are documented in the repository root: [../README.md](../README.md).

For local configuration, copy [`.env.local.example`](.env.local.example) to `.env.local`.
